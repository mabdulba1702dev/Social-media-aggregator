# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files (never commit real secrets)
.env
.env.local
.env.*.local

# supabase local dev
/supabase/.branches
/supabase/.temp

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# personal learning notes — not team-facing, never committed
swe.md
/notes/

# Baileys WhatsApp session — live auth credentials + synced contact data,
# never commit this under any circumstances (see docs/setup-guide.md §6)
worker/whatsapp-session/
whatsapp-session/
