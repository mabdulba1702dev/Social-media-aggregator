# components/ui

Empty on purpose — shadcn/21st.dev components land here via `npx shadcn@latest add ...` (see `docs/setup-guide.md` §7). Build order suggested in `docs/design-system.md`'s Component Inventory: Badge → Button → Input/Select → Sidebar nav item → Tag chip → Post card → Modal → Topbar.
